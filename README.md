# CSV-Data-Cleaner
A simple web app where users can upload a CSV file → automatically detect problems (missing values, duplicates, wrong datatypes, outliers, etc.) → fix them → and download the cleaned dataset.
